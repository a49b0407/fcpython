
CREATE TABLE 訂購
(訂單代號 INT NOT NULL,
產品代號 INT NOT NULL,
數量 INT,
PRIMARY KEY (訂單代號,產品代號),
FOREIGN KEY(訂單代號) REFERENCES 訂單(訂單代號) ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY(產品代號) REFERENCES 產品(產品代號) ON DEERE CSDCSDE SET NULL ON UPDATE CASCADE,
CHECK(數量 >=0 AND 數量< 100000));
